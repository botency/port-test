import React from "react";

const Resume = () => {
    return (
        <section className="flex flex-col justify-start items-start p-5 text-left">
            <div>Resume</div>
        </section>
    );
};

export default Resume;
